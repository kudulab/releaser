IDE_DOCKER_IMAGE="docker-registry.ai-traders.com/ai_ideide:0.1.1"
IDE_DOCKER_OPTIONS="--privileged"
# TODO: this must:
# * haver curl installed
# * have bats installed
# * copy .gitconfig.
# Better if it was alpine, because releaser docker image will be alpine too.
